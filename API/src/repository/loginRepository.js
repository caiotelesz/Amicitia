// import con from "./connection.js";

// export async function login(email, password) {
//   let command = `
//     SELECT * FROM tb_Login WHERE login_adm = ? AND senha_adm = ?
//   `

//   let resp = await con.query(command [email, password])
//   let info = resp[0];

//   return info;
// }

// export async function loginList(){
//   let command = `
//     SELECT * FROM tb_Login
//   `

//   let resp = await con.query(command, []);
//   let info = resp[0];

//   return info;
// }

// NO MOMENTO NN UTILIZAVEL